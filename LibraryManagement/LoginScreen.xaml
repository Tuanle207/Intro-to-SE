<Window 
        DataContext="{StaticResource LoginViewModel}"
        x:Class="LibraryManagement.LoginScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LibraryManagement"
        mc:Ignorable="d"
        KeyDown="Window_KeyDown"
        Title="Đăng nhập" Height="360" Width="540" WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize">
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="./Assets/backgroundLogin.jpg" Opacity="0.6"/>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>
        <Label Content="QUẢN  LÝ  THƯ  VIỆN" Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="28" FontWeight="Bold" FontFamily="Constantia"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Tài khoản" VerticalAlignment="Center" Margin="20 0" FontSize="18" FontWeight="Bold"/>
            <TextBox x:Name="tenTaiKhoan" Grid.Column="1" BorderThickness="2" Padding="5 0" BorderBrush="Black" FontSize="18" VerticalAlignment="Center" Text="{Binding AccountStaff,  UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}">
                <TextBox.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="15"/>
                    </Style>
                </TextBox.Resources>
            </TextBox>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Mật khẩu" VerticalAlignment="Center" Margin="20 0" FontSize="18" FontWeight="Bold"/>
            <PasswordBox x:Name="matKhau" Grid.Column="1" BorderThickness="2" Padding="5 0" BorderBrush="Black" FontSize="18" VerticalAlignment="Center" PasswordChanged="matKhau_PasswordChanged">
                <PasswordBox.Resources>
                    <Style TargetType="{x:Type Border}">
                        <Setter Property="CornerRadius" Value="15"/>
                    </Style>
                </PasswordBox.Resources>
            </PasswordBox>
            <TextBox x:Name="passWord" Grid.Column="1" BorderThickness="2" Padding="5 0" BorderBrush="Black" FontSize="18" VerticalAlignment="Center" Text="{Binding PasswordStaff,  UpdateSourceTrigger=PropertyChanged, ValidatesOnExceptions=True}" Visibility="Hidden">
            </TextBox>
        </Grid>
        <Button x:Name="btnLogin" Grid.Row="3" Content="Đăng nhập" Width="150" Command="{Binding LoginCommand}">

        </Button>
    </Grid>
</Window>
